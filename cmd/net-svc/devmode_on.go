//go:build devmode

package main

import "fmt"

// devMode включается ТОЛЬКО явной сборкой с тегом:
//
//	go build -tags devmode ./cmd/net-svc
//
// В этом режиме отключаются:
//   - проверка каталога, из которого подключается клиент (мера S2);
//   - отказ запуска при небезопасных правах каталога данных (мера S5).
//
// Нужен для разработки, когда двоичные файлы лежат в рабочем дереве, а не в
// %ProgramFiles%, и служба запускается от имени обычного пользователя.
const devMode = true

func init() {
	fmt.Println("╔══════════════════════════════════════════════════════════════╗")
	fmt.Println("║  ВНИМАНИЕ: сборка в режиме разработчика                      ║")
	fmt.Println("║  Проверки безопасности S2 и S5 ОТКЛЮЧЕНЫ.                    ║")
	fmt.Println("║  Не используйте эту сборку вне машины разработки.            ║")
	fmt.Println("╚══════════════════════════════════════════════════════════════╝")
}
